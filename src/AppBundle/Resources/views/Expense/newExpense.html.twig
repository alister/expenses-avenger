{% extends "AppBundle::layout.html.twig" %}

{% block title "Expenses" %}

{% block content_header '' %}

{% block content %}
    <h1 class="title">New Expense</h1>

    {%  if (form is defined) %}
    <form action="{{ url('post_expenses') }}" method="POST" {{ form_enctype(form) }}>

        {{ form_widget(form) }}

        <input type="submit" value="Submit">
    </form>
    {% endif %}

{% endblock %}
